# The generic package hook is intentionally not used here: it clones without
# submodules. This hook performs a recursive clone and records the pinned app
# and LVGL revisions in the installed image manifest.
packages/micropanel-touch-hook.sh|https://github.com/hackboxguy/micropanel-touch.git|d0f748e62101f61d1f3c285c8d91c3de22da2b94|/opt/micropanel-touch|

# Tier-2 domain-pack hooks are added below. Each pack must ship a menu fragment
# and handlers together and add its capability-matrix rows before it is enabled.
