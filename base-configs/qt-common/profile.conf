# qt-common base profile: shared base image for Qt-based boards
# (micropanel, pi4-touch-demo, qt-cluster-demo).
#
# The profile owns the base-stage parameters; boards referencing it via
# BASE_PROFILE=qt-common share one cached base image in
# <workspace>/base/profile-qt-common/.
EXTEND_SIZE_MB=3200
DEFAULT_PASSWORD=brb0x
